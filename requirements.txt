# Dependencias básicas para addons de Odoo (instaladas en Dockerfile)
xlsxwriter>=3.0.0
python-dateutil>=2.8.0
requests>=2.25.0

# Dependencias adicionales que se pueden instalar después si es necesario
# (comentadas para evitar problemas en el build inicial)
# num2words>=0.5.12
# qrcode[pil]>=7.0.0
# cryptography>=3.4.8
# pillow>=9.0.0

# Dependencias de desarrollo y testing
# pytest>=6.0.0
# pytest-odoo>=0.4.0

# Notas:
# - Las dependencias básicas están incluidas en el Dockerfile
# - Las dependencias adicionales se pueden instalar usando:
#   docker exec -it container-name pip install num2words qrcode[pil]
# - Para Railway, usar solo las dependencias básicas para evitar errores de build
